# Render Deployment Configuration (Blueprint)
# https://render.com/docs/blueprint-spec

services:
  - type: web
    name: server-control-system
    env: node
    plan: free
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
